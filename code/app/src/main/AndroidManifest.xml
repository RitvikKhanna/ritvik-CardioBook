<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ritvik_cardiobook1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/newicon"
        android:label="ritvik-CardioBook"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".oldMeasurementsCustomized"></activity>
        <activity android:name=".list_item" />
        <activity android:name=".addEditMeasurement" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>